@model Skill
<div class="row mt-2">
    <div class="col-10">
        @Html.DropDownListFor(x=>x.SkillId, ViewBag.skills as SelectList, "---Select---", new{@class="form-select form-select-sm"})
    </div>
    <div class="col-2">
        <a id="btnRemove" class="btn btn-danger btn-sm">Remove</a>

    </div>
</div>